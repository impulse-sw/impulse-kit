[package]
name = "impulse-tailwind-sources"
description = "Build-script helpers to feed Rust UI component sources into a Tailwind CSS build"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
authors = { workspace = true }

[dependencies]
