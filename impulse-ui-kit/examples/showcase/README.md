# Impulse UI Kit Showcase

Components gallery for Impulse UI Kit.

## Build

To build the showcase, run:

```bash
./build.sh
```

## Run

To run, you need to build [`impulse-static-server`](./../../../impulse-static-server/README.md) first. Also, you can use any static server instead.
