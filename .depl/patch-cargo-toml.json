{
  "patches": [
    {
      "files": [{"just": "Cargo.toml"}],
      "replace": {"from_to": ["\"panic-immediate-abort\", ", ""]}
    },
    {
      "files": [{"just": "Cargo.toml"}],
      "replace": {"from_to": ["immediate-abort", "abort"]}
    }
  ]
}
